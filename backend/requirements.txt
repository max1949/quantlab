# QuantLab AI 后端依赖 (Sprint 1 骨架)
# 业务相关库随对应 Sprint 增补。

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0  # EmailStr 校验

# 数据库 / ORM / 迁移
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# 异步任务 / 缓存
celery==5.4.0
redis==5.2.1

# 认证 (Sprint 1 用户系统)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # 锁版本: passlib 1.7.4 与 bcrypt>=4.1 的 __about__ 不兼容
pyjwt==2.10.1

# 计算 (engine 共享; 回测 Sprint 启用)
pandas==2.2.3
numpy==2.2.1
pyarrow==18.1.0  # Parquet 读写 (行情数据 V1)

# 测试 (开发期)
pytest==8.3.4
httpx==0.28.1
